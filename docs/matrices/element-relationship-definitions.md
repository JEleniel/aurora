# Element Definitions and Allowed Relationships

The canonical element mapping is in `schemas/element_definitions.json` and the relationship mapping is in `schemas/relationship-inverses.json`.

Use those JSON files as the canonical source of truth for UI lists, validation, and export rules. If you'd like, I can add an AJV schema or a CLI validator to enforce these relationships across the repo.
