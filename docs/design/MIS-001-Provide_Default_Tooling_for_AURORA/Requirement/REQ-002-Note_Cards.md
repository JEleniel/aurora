# REQ-002 (Requirement)

**Name:** Note Cards\

## Description

There must be a special card called `Note` that allows the capture of arbitrary text and annotates an existing node in the model without modifying it or the model. The `Note` card must have only one incoming link from the card it modifies.

## Links

_No outgoing links._
