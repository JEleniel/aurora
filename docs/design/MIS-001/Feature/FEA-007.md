# Feature : Compact Model Export Command

Provide a `compact` subcommand that writes a compact JSON snapshot of the model to a single file with top-level `{ "cards": [...] }`, where each card is stripped of `$schema` and `audit_trail` and each link is stripped of the `relationship` field. Output is deterministic (stable ordering) for diff-friendly automation.

## Attributes

None

## Links

- **enables**: ../Capability/CAP-007.json
- **satisfies**: ../Requirement/REQ-016.json


---

## Audit Trail

{{audit_trail}}

ID: FEA-007

