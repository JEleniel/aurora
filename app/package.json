{
	"dependencies": {
		"@material/web": "^1.0.0",
		"@tauri-apps/api": "^2",
		"@tauri-apps/plugin-opener": "^2"
	},
	"description": "AURORA is an architectural practice and tools designed to be equally followable by both machine and human agents.",
	"devDependencies": {
		"@sveltejs/adapter-static": "^3.0.6",
		"@sveltejs/kit": "^2.9.0",
		"@sveltejs/vite-plugin-svelte": "^6.2.1",
		"@tauri-apps/cli": "^2",
		"@types/node": "^25.0.2",
		"svelte": "^5.0.0",
		"svelte-check": "^4.0.0",
		"typescript": "~5.9.3",
		"vite": "^7.3.0"
	},
	"license": "GPL-3.0-or-later",
	"name": "aurora",
	"scripts": {
		"build": "vite build",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"dev": "vite dev",
		"preview": "vite preview",
		"tauri": "tauri"
	},
	"type": "module",
	"version": "1.0.0"
}
