# {mission.id}: {mission.name}

**Mission Statement**: {mission.description}

## Views

%% AGENT: Place links to generated views here, following the enclosed format. Remove the example.

- [{view name}]({relative link to view Markdown file})
%% AGENT: End of edit area.

## Cards

%% AGENT: Place links to all cards here here, sorted by `id`, in the enclosed example format, grouped by `card_type`. Remove the example.

### {card_type}

- [{id} ({card_subtype}): {name}]({relative link to Markdown card})
%% AGENT: Place links here, in the enclosed exampleformat, grouped by `card_type`. Remove the example.
