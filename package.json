{
  "name": "aurora",
  "version": "1.0.0",
  "description": "AURORA - Agent-Unified Representation Of Requirements And Architecture",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "cd frontend && pnpm dev",
    "build": "cd frontend && pnpm build && cd ../src-tauri && cargo build --release",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "lint": "cd frontend && pnpm lint",
    "check": "cd src-tauri && cargo check && cd ../frontend && pnpm check"
  },
  "workspaces": [
    "frontend",
    "src-tauri"
  ],
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  }
}
