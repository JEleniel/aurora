{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AURORA Link",
  "description": "A link represents a directional relationship between two cards. Links are untyped; their semantic meaning depends on the current view or interpretation context.",
  "type": "object",
  "properties": {
    "source_id": {
      "type": "string",
      "description": "ID of the source card (origin of the link)"
    },
    "target_id": {
      "type": "string",
      "description": "ID of the target card (destination of the link)"
    },
    "metadata": {
      "type": "object",
      "description": "Optional metadata about the link (weight, confidence, view-specific annotations)",
      "properties": {
        "weight": {
          "type": "number",
          "description": "Optional weight or strength indicator for the link"
        },
        "confidence": {
          "type": "number",
          "description": "Optional confidence level (0.0 to 1.0)"
        },
        "view_context": {
          "type": "string",
          "description": "Optional context indicating which view or interpretation applies to this link"
        }
      },
      "additionalProperties": true
    }
  },
  "required": ["source_id", "target_id"],
  "additionalProperties": false
}
