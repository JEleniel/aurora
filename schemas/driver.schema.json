{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AURORA Driver Card",
  "description": "A driver represents a high-level goal, concern, or rationale for the architecture. Drivers are aspirational and not directly verifiable.",
  "allOf": [
    {
      "$ref": "card.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "type": {
          "const": "driver"
        }
      },
      "required": ["type"]
    }
  ]
}
